# Repositories

This folder defines repository interfaces and domain mappers.

Rules:
- Map datasource DTOs to domain types here.
- Throw repository-specific errors for UI-safe handling.
- Keep feature slices isolated (feed, notifications, portfolio, etc.).
